{
	"type": "module",
	"devDependencies": {
		"alive-server": "^1.3.0"
	},
	"scripts": {
		"start": "npx http-server .",
		"dev": "npx alive-server --no-browser"
	},
	"dependencies": {
		"workbox-build": "^7.0.0"
	}
}
